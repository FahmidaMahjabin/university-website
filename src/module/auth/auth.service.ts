// step1: given id
// step2: defaultPassword

import { ILoginData } from './auth.interface'

// step3: createPassword ? if true then make new else login
const createLogIntoDB = async (data: ILoginData) => {
  console.log(data)
}
